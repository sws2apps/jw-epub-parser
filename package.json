{
  "name": "jw-epub-parser",
  "version": "1.0.3",
  "description": "",
  "author": "sws2apps",
  "license": "ISC",
  "scripts": {
    "prepatch": "npm version patch --no-git-tag-version",
    "patch": "git add . && git commit -m",
    "postpatch": "git push origin deploy",
    "preminor": "npm version minor --no-git-tag-version",
    "minor": "git add . && git commit -m",
    "postminor": "git push origin deploy",
    "premajor": "npm version major --no-git-tag-version",
    "major": "git add . && git commit -m",
    "postmajor": "git push origin deploy"
  },
  "dependencies": {
    "@gxl/epub-parser": "^2.0.4"
  }
}
